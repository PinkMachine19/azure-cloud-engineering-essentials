document.querySelectorAll('[data-check]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();const selected=new FormData(form).get('answer');const output=form.querySelector('[role=status]');output.textContent=selected===form.dataset.check?'Correct — '+form.dataset.correct:'Not quite. '+form.dataset.retry;}));
document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{const code=button.closest('.command').querySelector('code').textContent;await navigator.clipboard.writeText(code);button.textContent='Copied';setTimeout(()=>button.textContent='Copy command',1500);}));
