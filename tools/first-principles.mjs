const mapping = rows => '<div class="facts">' + rows.map(([label, text]) => `<div class="fact"><b>${label}</b><p>${text}</p></div>`).join('') + '</div>';
const scenario = (id, title, ordinary, flow, rows, cloud, takeaway) => `<section id="scenario-${id}"><h2>${id}. ${title}</h2><h3>Ordinary system first</h3><p>${ordinary}</p><pre><code>${flow}</code></pre>${mapping(rows)}<h3>Move part of it into cloud infrastructure</h3><p>${cloud}</p><div class="callout"><b>What stayed the same:</b> ${takeaway}</div></section>`;
export const firstPrinciples = `<article>
<p class="eyebrow">Layer 1 · Session 1 · Implemented</p><h1>Cloud From First Principles</h1>
<p class="lede">Start with a computer, then add communication, an application, and deployment. Cloud becomes easier to understand when you can already explain the system it hosts.</p>
<div class="meta">60–75 minutes. Conceptual walkthrough and prediction exercises. No installation, cloud account, or billable resources required.</div>
<h2>One model to carry through the course</h2>
<div class="callout"><b>A system is components that hold or transform state, exchange data or instructions, and operate under constraints and policies.</b></div>
${mapping([['Components','Machines, services, applications, databases, and devices: the parts doing the work.'],['State','Memory, files, database records, and configuration: what the system holds at a given moment.'],['Transformation','Computation, processing, and business logic: how inputs become outputs or state changes.'],['Exchange','Requests, responses, messages, events, and streams: how parts communicate.'],['Constraints / policies','Routing, identity, security, timing, reliability, and capacity: what is allowed and what is possible.']])}
<p>These are overlapping lenses, not mutually exclusive boxes. A routing table is state; a route also constrains where a packet can go. Communication alone does not explain computation, stored data, or the rules governing them.</p>
<h2>Learning objectives</h2><ul><li>Apply the same five lenses to local computing, networking, transport, applications, and deployment.</li><li>Distinguish reaching a host, reaching a process, and interpreting a request.</li><li>Explain what changes when a provider operates part of the system.</li><li>Reduce a service catalog to recurring engineering needs.</li></ul>
<div class="toc"><a href="#scenario-1">1. Local machine only</a><a href="#scenario-2">2. Ordinary networking</a><a href="#scenario-3">3. Transport/session</a><a href="#scenario-4">4. Application runtime</a><a href="#scenario-5">5. Bootstrap/deployment</a><a href="#cloud-overlay">When cloud enters</a></div>
${scenario(1,'Local machine only','A person writes a note and saves it to a local file. There is no network in this scenario. The application asks the operating system to write bytes; the filesystem organizes them and the storage device persists them.','Application → OS/filesystem → storage',[
['Components','Editor, operating system, filesystem, storage device.'],['State','The text in memory, file contents, directory entries, and file permissions.'],['Transformation','Encoding the text into bytes and updating filesystem metadata.'],['Exchange','Function calls, system calls, buffers, and device instructions inside the machine.'],['Constraints / policies','File permissions, available disk space, valid paths, and write errors.']],
'Now run the editor on a provider-hosted machine and save to its attached disk. The local save path still exists inside that machine; remote access adds a separate network path. Using a managed object store instead changes the save interface to an API and introduces network and service failures.',
'Components still transform and persist state under rules. Exchange does not require a network. A successful save also does not, by itself, prove that a backup exists.')}
${scenario(2,'Ordinary networking','Leave application behavior out for now. Follow a packet between machines. A network interface emits frames; a switch or wireless access point connects a local link; a router forwards between networks. A firewall may filter traffic along the way.','Machine → NIC → switch/Wi-Fi → router/firewall → network → destination machine',[
['Components','Machines, network interface cards (NICs), switches, access points, routers, and firewalls.'],['State','IP addresses, ARP/neighbor tables, routing tables, and firewall connection state.'],['Transformation','Encapsulation, forwarding, filtering, and network address translation (NAT) where configured.'],['Exchange','Frames on a link and IP packets across networks.'],['Constraints / policies','Routes, access control lists (ACLs), firewall rules, VLANs, subnets, and maximum transmission unit (MTU).']],
'Move one machine into a provider network. A cloud virtual network (VNet/VPC) is provider-managed, software-defined networking built on these same ideas. You configure address spaces, subnets, routes, and traffic policy through a control interface while the provider operates the underlying fabric.',
'Addressing, reachability, forwarding, and filtering still matter. The diagram is a possible routed path: same-subnet traffic may not traverse a router, NAT is optional, and IPv6 uses neighbor discovery rather than ARP. Cloud networks need not expose every physical-network feature.')}
${scenario(3,'Transport/session','Two machines can have a valid network path without a program on the destination being ready to receive traffic. Transport adds endpoint identification and delivery behavior.','Destination IP + TCP/UDP + destination port → receiving socket/process',[
['Components','Hosts, operating-system transport stacks, and sockets used by processes.'],['State','Bound ports, socket buffers, TCP connection state, sequence numbers, and timers.'],['Transformation','Multiplexing/demultiplexing, TCP ordering and retransmission, and checksum verification.'],['Exchange','TCP segments or UDP datagrams carried inside IP packets.'],['Constraints / policies','Listening endpoints, permitted ports, timeouts, connection limits, and protocol choice.']],
'Move the receiving process to a provider-hosted machine. IP addresses, transport protocols, and ports remain relevant. A load balancer or proxy may add an intermediary; a terminating proxy creates a separate connection to the backend.',
'Network finds the machine. Transport finds the process. Application defines the meaning. More precisely, routing reaches an interface or endpoint, and the OS uses protocol, addresses, and ports to select a socket. TCP offers a reliable ordered byte stream, not message boundaries; UDP does not promise delivery or ordering. An application login session is separate from a TCP connection.')}
${scenario(4,'Application runtime','Now send a request to an application on an organization-operated server. After bytes reach its socket, the application interprets the protocol, checks the request, performs work, and produces a response.','User device → network → destination machine → transport/port → application → response',[
['Components','Client, server, application process, and any database or downstream service.'],['State','Memory, files, application sessions, configuration, and database records.'],['Transformation','Parsing, validation, application/business logic, and data updates.'],['Exchange','Requests, responses, and messages with application-defined meaning.'],['Constraints / policies','Authentication, authorization, protocols, timeouts, validation, and capacity.']],
'Run that application on a VM, in a container, on a managed app platform, or in a serverless runtime. A provider may handle more of the host, scheduling, scaling, or ingress. Serverless still uses machines; their lifecycle is abstracted from you.',
'The application still receives inputs, changes or reads state, and returns outputs under rules. Hosting choices change operational responsibility and may change persistence, execution limits, concurrency, and failure behavior; they do not remove the interaction model.')}
${scenario(5,'Bootstrap/deployment','Before the application handles a request, something has to build and start it. On your own server, a deployment tool can read source/configuration, build a package, copy it to a host, configure a service, and start the process.','Source/config → deployment tool/control plane → target environment → application starts',[
['Components','Repository, build runner/pipeline, deployment tool or control plane, and host/cluster.'],['State','Source, artifacts, configuration, desired deployment state, and observed deployment state.'],['Transformation','Build, package, provision, configure, and start or reconcile the runtime.'],['Exchange','API calls, artifacts, manifests, and secret values or references through authorized channels.'],['Constraints / policies','Identity, permissions, deployment rules, approval gates, quotas, and version compatibility.']],
'A pipeline calls provider control-plane APIs to provision resources and deploy an artifact. The provider checks permissions and quotas, records desired state, and arranges the runtime. You still supply the intended configuration and verify that the application actually becomes healthy.',
'Bootstrap itself consists of components exchanging state and instructions under rules. A deployment request being accepted is not the same as a healthy application serving users.')}
<h2>Runtime and bootstrap belong to the same model</h2><div class="grid"><div class="card"><h3>Runtime</h3><p>Components doing business work: accepting a document, storing its metadata, or returning a result.</p></div><div class="card"><h3>Bootstrap / control</h3><p>Components creating and configuring the system that will do the business work: building a package, allocating a host, or starting a process.</p></div></div><p>Both hold state, transform it, exchange instructions, and enforce rules. They have different responsibilities and often different identities. A deployment identity may create a resource while the runtime identity can only read its data.</p>
<h2 id="cloud-overlay">When does cloud actually enter the picture?</h2>
<p>Cloud does not introduce a new fundamental systems layer. In this course’s public-cloud framing, it enters when some machines, networks, storage, control planes, runtimes, or services are owned or operated by a provider rather than directly by your organization. On-demand APIs, pooled capacity, elasticity, and measured usage shape how you obtain and operate those capabilities; renting a remote machine alone does not describe every cloud characteristic.</p>
<pre><code>Local machine → ordinary networking → transport/session
→ application runtime → bootstrap/deployment</code></pre>
<p><b>Now move some of those components into provider-owned infrastructure.</b> This is a learning progression, not a literal protocol stack: bootstrap creates and configures the runtime rather than sitting after an application request.</p>
<figure class="diagram"><div class="grid"><div class="card"><h3>Pre-cloud / ordinary runtime</h3><p>Device<br>↓<br>Network<br>↓<br>Transport<br>↓<br>Application<br>↓<br>State/Data</p></div><div class="card"><h3>Bootstrap / control</h3><p>Source/Config<br>↓<br>Build/Deploy/Provision<br>↓<br>Runtime Environment</p></div><div class="card"><h3>Cloud overlay: one example</h3><p>Organization: device + source/config</p><p>↓ network and control API calls ↓</p><div class="callout"><b>Provider-operated boundary</b><p>Network + transport<br>↓<br>Application runtime<br>↓<br>State/Data</p><p>Managed control plane → runtime environment</p></div></div></div><figcaption>Some boxes move behind a provider boundary and are exposed through managed services and APIs. This is a conceptual dependency view, not a packet trace. Your application code, data decisions, and access configuration still carry responsibilities.</figcaption></figure>
<p>Cloud networking is still networking. Cloud compute is still compute. Cloud storage is still storage. Cloud deployment is still components exchanging instructions and state through APIs and control planes.</p>
<h2>Read the catalog as recurring needs</h2><figure class="diagram"><div class="facts">${[['Run it','Execute computation.'],['Connect it','Exchange data across boundaries.'],['Store it','Retain and retrieve state.'],['Secure it','Establish identity and enforce access.'],['Observe it','Measure behavior and diagnose failures.'],['Keep it alive','Handle faults, recovery, and capacity.'],['Deploy/configure it','Create and change the system repeatably.']].map(([a,b])=>`<div class="fact"><b>${a}</b><p>${b}</p></div>`).join('')}</div><figcaption>Seven recurring capability needs. A product may serve several needs, and one need may require several products.</figcaption></figure>
<p>The cloud catalog may contain hundreds of product names, but those products largely implement a relatively fixed set of recurring system needs. Vendors expand these needs into services with different interfaces, guarantees, responsibility boundaries, costs, and limits. Ask which need a service meets and what you still have to operate before memorizing its name.</p>
<h2>Check your mental model</h2><div class="question"><b>A packet reaches a server, but no process is listening on the destination port. Has the application received a request?</b><details><summary>Reveal</summary><div>No. Host reachability does not prove a listening socket exists, or that an application can understand and successfully handle the request.</div></details></div><div class="question"><b>A deployment API accepts your configuration. Does that prove the runtime is healthy?</b><details><summary>Reveal</summary><div>No. The control plane may still be provisioning, the process may fail to start, or dependencies may be unreachable. Check deployment state and runtime health separately.</div></details></div><form class="question" data-check="b" data-correct="The system model remains; ownership, interfaces, and operational responsibility change." data-retry="Look for components, state, transformation, exchange, and rules in both versions."><b>What changes when an application moves to a managed runtime?</b><p><label><input type="radio" name="answer" value="a"> State, transport, and deployment stop existing.</label><br><label><input type="radio" name="answer" value="b"> The provider operates more components through managed interfaces.</label></p><button>Check</button><p role="status"></p></form>
<h2>Practice without deploying anything</h2><p>Choose an application you know. Draw its runtime path and its deployment path separately. For each, name components, state, transformations, exchanges, and policies. Circle the parts you could move to a provider and write down which responsibilities would stay with your team.</p>
<h2>Architecture delta</h2><p>No infrastructure changed. You now have a model for explaining both the running system and the mechanisms that create it.</p>
<h2>Technical references</h2><ul><li><a href="https://www.rfc-editor.org/rfc/rfc9293.html">RFC 9293: Transmission Control Protocol</a></li><li><a href="https://www.rfc-editor.org/rfc/rfc768.html">RFC 768: User Datagram Protocol</a></li><li><a href="https://csrc.nist.gov/pubs/sp/800/145/final">NIST: The Definition of Cloud Computing</a></li></ul>
<h2>Carry this forward</h2><div class="callout"><p><b>Core model:</b> A system is components that hold or transform state, exchange data or instructions, and operate under constraints and policies.</p><p><b>Cloud:</b> The same model, with some components and control mechanisms moved into provider-owned, software-defined infrastructure.</p><p>Now that the system itself makes sense, we can start learning the Azure services that implement these needs.</p></div>
</article>`;
